export class UserForm {
    username!:string;// Property to store the username entered by the user
    password!: string; // Property to store the password entered by the user
    reEnterPassword!: string; // Property to store the re-entered password for confirmation
    
}
